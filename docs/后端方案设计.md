# 课程管理系统 方案设计（草案 v3）

> 日期：2026-09-17
> 状态：**基本盘已定** —— 本文只做方案讨论，未开始实现
> v1 → v2：项目定位从「课时统计工具的服务器版」升级为**课程管理系统**
> v2 → v3：技术选型（FastAPI）与账号体系（管理员建号）**已确认**；理清「课时」的三种含义

---

## 〇、版本演进

| 项 | v1 | v2 | **v3（已定）** |
|---|---|---|---|
| 项目定位 | 课时统计工具 + 后端 | 课程管理系统 | 同 v2 |
| 后端框架 | 待定 | FastAPI / Django 待定 | **FastAPI ✅** |
| 登录标识 | 用户名 | 手机号 | **手机号 ✅** |
| 账号来源 | 邀请码自助注册 | 待确认 | **管理员建号 ✅**（无邀请码） |
| 学生 | 曾考虑自助打卡 | 不参与系统 | 同 v2 ✅ |
| 角色 | 管理员 / 老师 | 超管 / 管理员 / 老师 | 同 v2 ✅ |
| 课时模型 | — | 待确认 | **见第七节「三种课时」✅** |

---

## 一、项目定位

从「一个人算工资的小工具」升级为「**培训机构的课程管理系统**」：

- **老师**：看课表 → 签到考勤 → 看自己的课时和课时费 → 导出自己的考勤表
- **管理员**：管老师 / 学生 / 班级 / 排课 → 审批变更 → 导出所有人的考勤表
- **超管**：以上全部 + 管理管理员账号

桌面版（现有 v0.3）**逐步退役**，最后只保留「算课时 + 导考勤表」这个功能——详见第九节。

---

## 二、角色与权限

| 角色 | 权限范围 |
|---|---|
| **超管** | 全部权限 + **添加 / 修改 / 删除管理员账号** |
| **管理员** | 老师全部权限 + 管学生 / 班级 / 排课 + 审批 + 导出所有人的考勤表 + 管老师 |
| **老师** | 看自己课表、考勤签到、看学生 / 班级（**只读**）、看自己的课时费、导出自己的考勤表 |

> **超管的产生**：系统初始化时用脚本直接在服务器上建一个超管账号，之后所有管理员都由超管在界面上开。这样就不用纠结「第一个管理员从哪来」。

**权限设计原则**：老师能看的，管理员都能看；管理员能改的，老师只能看。界面上按角色显示不同按钮，**同一套页面**。

---

## 三、功能模块

### 页面结构

```
┌────────────────────────────────────────────────────┐
│  [Logo] 课程管理系统          用户管理 ▾  退出登录  │  ← 顶栏
├──────────┬─────────────────────────────────────────┤
│ 首页      │                                         │
│ 查看课程  │                                         │
│ 学生管理  │            主 内 容 区                   │
│ 班级管理  │                                         │
│ 考勤管理  │                                         │
└──────────┴─────────────────────────────────────────┘
   左侧导航栏
```

### 各模块说明

| 模块 | 老师 | 管理员 | 超管 |
|---|---|---|---|
| **首页** | 欢迎页：今日课程、待办提醒 | 同左 + 全局概览 | 同左 |
| **用户管理**（右上角） | 改自己的密码 / 手机号 | 同左 | 同左 + 管管理员账号 |
| **查看课程** | 默认今天，可切**月视图**日历；点击某节课看详情、完成考勤；改时间需提审 | 同左 + 直接改（免审批） | 同左 |
| **学生管理** | 只读：姓名 / 性别 / 是否成年 / 所属班级 / 出勤 / **所剩课时** | + 增删改 | 同左 |
| **班级管理** | 只读：班级信息、已上多少课时 | + 增删改 | 同左 |
| **考勤管理** | 自己的课时、课时费；导出自己的考勤表 | + 查看所有人、导出所有人、增删改老师 | 同左 |

### 审批流（改课程时间）

```
老师发现某节课时间要变
   ↓
提交修改申请（填新时间 + 原因）
   ↓
任一管理员审批 → 通过 / 驳回
   ↓
通过则课程时间生效，留痕
```

**管理员自己改 → 直接生效，不用审批。**

> 你说得很对：这个审批「只是走个流程」，但它的价值是**让管理员知情、能担责任**。所以设计上要**轻**——一个待办列表 + 通过/驳回两个按钮就够了，不要做成复杂的工作流引擎。

---

## 四、品牌主色

**主色：`#a9c5b3`**（palmsugar 系列项目的统一主色调，柔和鼠尾草绿）

建议配色体系：

| 用途 | 色值 | 说明 |
|---|---|---|
| 主色 | `#A9C5B3` | 按钮、导航栏高亮、主标识 |
| 主色-深 | `#8BAF9B` | hover / 按下态 |
| 主色-浅 | `#E8F0EA` | 页面背景、卡片底 |
| 主色-文字 | `#2F3E36` | 深墨绿，用于正文（比纯黑柔和） |
| 强调色 | `#D98C7A` | 警示 / 删除按钮（暖色对比） |

> 备注：这套是**界面**配色。Excel 导出里那 12 个班级配色是另一回事——那里的目标是「**色差大、易区分**」，和品牌色不是一个目的，不要混用。

---

## 五、技术选型

| 层面 | 选型 |
|---|---|
| 后端 | **FastAPI**（Python） |
| 数据库 | **SQLite**（WAL 模式） |
| ORM | **SQLModel** |
| 前端 | **uniapp**（Vue 3）→ 一套代码出 H5 网页 + 微信小程序 |
| 认证 | JWT + bcrypt |
| 部署 | 腾讯云 + Nginx 反向代理 |

### 回答你的 Q1：FastAPI 还是 Django？

先澄清一个关键误解：

> **「以后学生/老师变多，要不要换框架」——这个担心不成立。**

FastAPI 和 Django 处理几千上万用户都没问题，**框架不是瓶颈**。真正决定能扛多少人的是**数据库**（SQLite → PostgreSQL），跟框架无关。所以不存在「人多了得转 Django」这件事。

再说你担心的两点：

**① 前期自己写管理界面，代价大吗？——中等，但比看起来小。**

关键原因：**你本来就要求老师能看学生和班级**（只读）。这些页面**无论如何都要用 uniapp 写一遍**。管理员无非是在同一套页面上多几个「新增 / 编辑 / 删除」按钮——边际成本很小，而且是复制粘贴的活。

**② 后期维护 Django 会不会更麻烦？——会，因为你会得到两套前端。**

选 Django 的话：
- ✅ 白送一个管理后台（`/admin`），录数据不用写界面
- ⚠️ 但那个后台**风格老旧、手机上很难用**，只适合你在电脑上录数据
- ⚠️ 于是你有两套前端、两套登录、两套权限模型，老师那边还是要用 uniapp 再写一遍
- ⚠️ 审批流要同时出现在两边，容易乱

**③ FastAPI 转 Django 麻烦吗？——很麻烦，等于重写。**

但结论是：**你不会因为「人多」需要转**，所以这个担心可以放下。

### ✅ 结论：已确认用 FastAPI

**决策理由**（用户 2026-09-17 确认）：**框架选择跟人数无关**，而 Django 会带来两套系统的运维负担。故选 FastAPI：单前端、单登录、单权限，管理界面复用教师界面加按钮。

> Django 因为会导致两套系统运维麻烦，已被排除。

---

## 六、整体架构

```
┌──────────────┐   ┌──────────────┐
│ 微信小程序    │   │  H5 网页版    │
│ （后期）      │   │ （先做）      │
└───────┬──────┘   └──────┬───────┘
        └────────┬────────┘
                 │  同一套 uniapp 代码
                 │  HTTPS + JSON
                 ▼
      ┌──────────────────────┐
      │  Nginx（443）         │ ← 与现有项目共用，
      │  按子域名分流          │   jiaowu.palmsugar.cn
      └──────────┬───────────┘
                 ▼
      ┌──────────────────────┐
      │  FastAPI              │
      │  认证 / 业务 / 审批    │
      │  定时任务（清理+备份）  │
      │  ★ Excel 导出复用现有  │
      └──────────┬───────────┘
                 ▼
      ┌──────────────────────┐
      │  SQLite (app.db)      │
      └──────────────────────┘
```

> **一个好消息**：现有的 `excel_exporter.py`（openpyxl 生成考勤表）**几乎可以原样搬到服务端**——老师点「导出」，服务端生成 xlsx 返回下载。这部分逻辑不用重写。

---

## 七、数据库设计

| 表 | 用途 |
|---|---|
| `users` | 超管 / 管理员 / 老师 |
| `classes` | 班级 |
| `students` | 学生（**不是用户**，只是数据） |
| `class_students` | 班级-学生 多对多 |
| `lessons` | 课程（排课） |
| `attendance` | 出勤记录 |
| **`hour_transactions`** | **学生课时流水（购买 / 消耗 / 调整）** |
| `approvals` | 审批记录 |
| `rates` | 班级费率 |

> 没有 `invite_codes` 表——已改为管理员建号，不需要邀请码。

### users

| 字段 | 说明 |
|---|---|
| id | 主键 |
| phone | **登录标识**，唯一 |
| display_name | 显示名（真实姓名） |
| password_hash | bcrypt，**不存明文** |
| role | `super_admin` / `admin` / `teacher` |
| is_active | 停用而非删除 |
| must_change_password | 首次登录强制改密（管理员发的初始密码） |
| created_at | |

### students

| 字段 | 说明 |
|---|---|
| id | |
| name | 姓名 |
| gender | 性别 |
| is_adult | 是否成年 |
| phone | 联系电话（可选） |
| note | |
| is_active | |

> ⚠️ **注意**：学生表里**没有** `remaining_hours` 字段。余额由流水算出，见下方「三种课时」。

### lessons / attendance / approvals

- `lessons`：id / class_id / teacher_id / date / start_time / hours / status（正常 / 已改期 / 已取消）
- `attendance`：id / lesson_id / student_id / status（出勤 / 请假 / 缺勤）/ 记录时间
- `approvals`：id / 申请类型 / 目标 id / 申请内容(JSON) / 申请人 / 审批人 / 状态 / 时间

### hour_transactions（学生课时流水）

| 字段 | 说明 |
|---|---|
| id | |
| student_id | 哪个学生 |
| type | `purchase` 购买 / `consume` 消耗 / `adjust` 手动调整 |
| amount | 正数（购买/调整）或负数（消耗） |
| lesson_id | 因哪节课消耗的（购买时为 NULL） |
| note | 备注 |
| created_by | 操作人 |
| created_at | |

**学生的所剩课时 = 该学生所有流水求和。** 不存冗余字段——涉及钱，必须能查出每一笔的来源。

---

## 七之二、⚠️ 三种「课时」必须分清

这是整个系统**最容易搞混、最容易对不上账**的地方。系统里有三种完全不同的「课时」：

| # | 概念 | 归属对象 | 计算方式 | 用途 |
|---|---|---|---|---|
| **1** | **教师课时费** | **老师** | 上了多少节课 × 时薪 | 算工资、导考勤表（= 现有桌面版那套） |
| **2** | **班级已上课时** | **班级** | 这个班累计上过多少次课 | 班级管理页展示 |
| **3** | **学生所剩课时** | **每个学生各算各的** | 购买 − 消耗（流水求和） | 学生管理页展示、家长查询 |

### 为什么学生课时必须独立于班级

因为实际情况是：

- 有学生**只买几节课**（不跟班级的完整课时包走）
- 有学生**同时参加两个班级**

所以**学生的课时余额和班级的课时数天生就不一样**。如果把余额挂在班级上，**一定对不上**。

> **✅ 已确认（用户 2026-09-17）：每个学生一条独立的课时余额，按学生单独扣。**

### 扣课时的规则（已确认）

```
一次课正常上了
   → 班级的「已上课时」+1
   → 老师的「课时费」+1 节课
   → 该班每个学生的「所剩课时」各扣 1 次   ← 每人扣自己的
```

**关键点**：

| 情况 | 扣不扣课时 | 说明 |
|---|---|---|
| 正常上课 | ✅ 扣 | 全班学生都扣 |
| 学生**请假** | ✅ **照样扣** | 你说得明确：「就算一个学生请假，也算课时」 |
| 学生缺勤/旷课 | ✅ 照样扣 | 同上逻辑（待你确认） |
| **课程取消**（这次课不上） | ❌ **不扣** | 唯一的例外 |

> **重要推论**：出勤状态（出勤 / 请假 / 缺勤）**不影响扣课时**，它只是**记录**（谁来了谁没来）。
> 真正决定扣不扣的是**课程本身有没有上**（`lessons.status`）。

**实现方式**：课程标记为「已上课」时，一次性给该班所有学生各写一条 `consume` 流水。课程若取消，则作废这批流水（或直接不生成）。

---

## 八、接口清单（初稿）

### 认证与账户

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 手机号 + 密码 → token |
| POST | `/api/auth/change-password` | 改自己的密码 |
| PATCH | `/api/users/me` | 改自己的手机号 / 显示名 |

### 管理员

| 方法 | 路径 | 说明 |
|---|---|---|
| POST/GET/PUT/DELETE | `/api/admin/teachers` | 老师账号增删改查 |
| POST/GET/PUT/DELETE | `/api/admin/admins` | **管理员账号管理（仅超管）** |
| POST | `/api/admin/reset-password/{id}` | 重置某人的密码 |

### 业务

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/lessons?date=` / `?month=` | 按日/按月取课程 |
| GET | `/api/lessons/{id}` | 课程详情 + 学生名单 |
| POST | `/api/lessons/{id}/attendance` | 提交考勤 |
| POST | `/api/lessons/{id}/reschedule` | 申请改期（老师提交审批） |
| PUT | `/api/lessons/{id}` | 直接改（管理员，免审批） |
| GET/POST/PUT/DELETE | `/api/students` | 学生管理 |
| GET/POST/PUT/DELETE | `/api/classes` | 班级管理 |
| GET | `/api/attendance/my?month=` | 我的课时与课时费 |
| GET | `/api/attendance/export?month=&teacher=` | **导出考勤表 Excel** |
| GET/POST | `/api/approvals` | 审批列表 / 提交 |
| POST | `/api/approvals/{id}/approve` \| `/reject` | 审批操作 |

> FastAPI 自动生成在线接口文档，浏览器打开 `/docs` 就能点着测——**这对你验收特别方便**。

---

## 九、桌面端怎么办

现有 v0.3 桌面版**逐步退役**，最终只保留**「算课时 + 导考勤表」**这一个功能（就是现在交给领导的那张表），其余全部转到后端。

> ⚠️ **硬性约束**：桌面版 exe **必须保留到网页版和小程序上线为止**。你每月要向领导提交工资报表，目前完全依赖它——在网页版和小程序上线并验证稳定之前，它是唯一的生产工具。
>
> - 不要删除或覆盖 `codes/dist/` 下的历史 exe
> - 打包新版本继续用不同 `--name`（如 `SalaryCounter-v0.3.exe`），保留历史版本共存
> - 「退役」的前提是**网页版和小程序已上线且你确认稳定**，不是「开发完成」

---

## 十、部署：服务器与域名

### 服务器：现有 4核4G40G 够用，不用再租

| 考虑 | 结论 |
|---|---|
| 负载 | 两个低流量项目（刷题背单词站 + 本系统）+ 几个老师，**绰绰有余** |
| 资源占用 | FastAPI + SQLite 内存占用**极小**（几十 MB 量级） |
| 是否再租 | **不必**。35/月 那台 2核2G 也够，但没必要——多花钱、多一台要维护 |
| 何时该升级 | 用 `top` 看内存/CPU 长期吃紧、带宽跑满，或想隔离故障风险时 |

**建议：先用现有的。真不够了再升配迁移。** 迁移成本不高（SQLite 就是拷文件），不必提前买。

### 域名：一个一级域名挂所有项目，标准做法

```
palmsugar.cn                    ← 你已备案的一级域名
├── www.palmsugar.cn            ← 主站
├── jiaowu.palmsugar.cn         ← 本系统（课程管理系统）
└── quiz.palmsugar.cn           ← 现有的刷题背单词站
```

全部指向同一台服务器，**Nginx 按子域名分流**。子域名数量**无限制**，这是标准做法，不花额外钱。

**要不要分开两台服务器？** 技术上两种都可以（子域名指向不同 IP 即可）。分开的好处是**故障隔离**（一台挂了不影响另一台），坏处是多花钱、多维护。**以你的体量，一台就够了。**

> ⚠️ **备案注意**：ICP 备案是绑定「**域名 + 接入商（服务商）**」的。**换服务器或新增服务器时，需要做「备案接入」变更**，否则域名可能被断。这是很多人踩的坑。

---

## 十一、关于微信小程序

### 一个微信账号能挂多少个小程序？

| 主体类型 | 可注册数量 |
|---|---|
| **个人** | **5 个** |
| 个体工商户 | 5 个 |
| **企业 / 政府 / 媒体 / 其他组织** | **50 个** |

补充：**一个邮箱只能注册 1 个小程序**；一个手机号最多绑定 5 个；同一主体下的所有小程序（含已注销、封禁的）都计入上限。

### 小程序的两个硬性门槛

1. **必须 ICP 备案** —— 且**备案主体要与小程序认证主体一致**（个人备案不能用于企业类小程序）。备案流程：提交 → 平台初审（1~2 工作日）→ 工信部短信核验 → 通管局审核（**1~20 个工作日**）
2. **必须 HTTPS** —— HTTP 被全面禁用；证书要有效、TLS 1.2+、域名与后台登记完全一致
3. **域名要在微信后台配置**（`request` 合法域名等），还要放校验文件到网站根目录

> ⚠️ 有一类小程序有个限制：**个人主体不能使用 `web-view`**。不过 uniapp 是**编译成原生小程序代码**（不是 web-view 套壳），所以不受影响。

### 你的思路：先网页、后小程序 —— **完全正确** ✅

理由：

1. **网页版能快速验证功能**，改一版几分钟，不用等审核
2. **uniapp 同一套代码**，后面编译成小程序，**前端代码基本复用**
3. **小程序有备案 + 审核周期**，放后面不会卡住前面的进度
4. 前一个项目小程序也没做——**先跑通一个，再复制经验**

**唯一要提前准备的**：如果这个系统最终要上小程序，**备案主体最好和你的小程序主体一致**，别到时候才发现对不上。这个可以现在就想想。

---

## 十二、分期计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| **Phase 0** | 域名解析 + 服务器环境 + 项目骨架 | 后端能跑 |
| **Phase 1** | 后端：登录 + 用户/角色 + 老师建号 | `/docs` 能点着测 |
| **Phase 2** | uniapp 网页版：登录 + 课程（日/月视图）+ 考勤 | **老师能真正用起来** |
| **Phase 3** | 学生管理 + 班级管理 + 课时余额 | 补齐学生/班级 |
| **Phase 4** | 考勤管理 + 导出 Excel（复用现有导出逻辑） | **能交报表了** |
| **Phase 5** | 审批流（改期申请） | 补齐审批 |
| **Phase 6** | 微信小程序 | 编译上线 |

**关键节奏**：**Phase 4 结束时就已经能替代桌面版了**（能交报表）。Phase 5、6 是锦上添花。

> **关于 9/20 给老板看**：用现在的 v0.3 桌面版演示，后端作为「后续规划」讲。别赶。

---

## 十三、决策点状态 —— 基本盘已定 ✅

| # | 决策点 | 结论 |
|---|---|---|
| 1 | **后端框架** | ✅ **FastAPI**（Django 会导致两套系统运维麻烦，排除） |
| 2 | **账号体系** | ✅ **管理员建号**，发初始密码。**不做邀请码、不做短信验证码** |
| 3 | **登录标识** | ✅ **手机号** + 密码（老师 ≤20 人，换号由管理员改，工作量可接受） |
| 4 | **桌面端去向** | ✅ 逐步退役，只保留「算课时 + 导考勤表」 |
| 5 | **课程与课时** | ✅ 课程为源头，排课 + 签到自动算课时费 |
| 6 | **学生课时** | ✅ **每个学生独立余额**，流水记账（`hour_transactions`） |
| 7 | **扣课时规则** | ✅ 课上了就扣（请假也扣），**只有课程取消才不扣** |
| 8 | **服务器与域名** | ✅ 用现有 4核4G40G，**不必再租**；一级域名挂子域名 |

### 为什么不做邀请码

| | 邀请码自助注册 | **管理员建号 ✅** |
|---|---|---|
| 表 | 多一张 `invite_codes` | 不需要 |
| 流程 | 生成码 → 发码 → 老师自己注册 | 建号 → 发初始密码 |
| 界面 | 注册页 + 邀请码管理页 | 老师管理页（本来就要有） |
| 老师体验 | 要自己填一堆 | 收到就能用 |

**「管理员建号」同样满足「只开放给内部老师」，还少一半东西。** 用户 2026-09-17 确认采用。

### 唯一还需你点头的小点

**学生缺勤/旷课是否也扣课时？**
- 已确认：**请假照样扣**
- 推测：**缺勤（旷课）也应照样扣**（同属「课上了但学生没来」）
- 若你们有不同规定，告诉我改。

> 其余前期讨论中的挂起项（登录注册、账户管理、设置界面、课程管理、小程序、跨项目联动）**均已在本方案中收敛**。

---

## 十四、风险与注意事项

| 风险 | 应对 |
|---|---|
| **备案周期长**（1~20 工作日） | 若要上小程序，**尽早启动** |
| 备案主体与小程序主体不一致 | 现在就确认主体类型，别到审核才发现 |
| 换服务器触发备案变更 | 尽量先定好服务器再备案接入 |
| 学生课时余额对不上账 | 用流水记录（方案 B），可追溯 |
| 范围膨胀 | 严格按 Phase 推进，每期都可独立验收 |
| 单人开发 | 每期切小，做一期算一期 |

---

## 十五、下一步

基本盘已定，下一步：

1. 你确认本文（尤其第十三节最后一个待点头的小点：**缺勤是否扣课时**）
2. 我按结论细化成《实施计划》——具体到文件结构、接口签名、表结构 DDL
3. 从 Phase 0 开始动手（域名解析 + 服务器环境）

> **本文仅为方案讨论稿，未写任何代码。**

---

## 附：关于把项目移到 `D:\WorkSpace\projects\mix`

**移动步骤**（你自己做，很简单）：

```bash
cd /d/WorkSpace/projects
mv salary-counter mix/
```

然后在新的路径下重新打开 Claude Code 即可。

**安全性检查已做**：
- ✅ 仓库里**没有绝对路径依赖**（只有 `docs/NEEDS.MD` 里一个失效的图片链接）
- ✅ git 不关心目录位置，`.git` 跟着一起走，历史完整
- ✅ 构建产物 `codes/build/`、`codes/dist/` 已在 `.gitignore` 中，移动无影响

**唯一影响**：**Claude Code 的项目记忆目录名会随路径变化**（现在是 `d--WorkSpace-projects-Python-salary-counter`），移完相当于换了个记忆空间。现有 6 条记忆见 `MEMORY.md`，如需保留可先导出。
