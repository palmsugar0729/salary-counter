# 开发日志

## 2026-09-17 — v0.3 测试反馈修订 + 打包

### 完成事项

- 用户本地测试 v0.3，反馈「基本没有问题」，仅一项调整
- **Excel 不导出班级名**：导出改回 10 列（A~J），「班级」列仍显示班级类型；班级名保留在预览表 / 配色 / 筛选中使用
- 重新打包 `SalaryCounter-v0.3.exe`

### 关键决策

- Excel 导出保持 10 列，不加班级名列 —— 班级名是"内部分组用"的字段，导出表格交给领导看时不需要、也不希望多一列

### 文件变更

| 文件 | 操作 | 说明 |
|---|---|---|
| `codes/excel_exporter.py` | 修改 | HEADERS 去掉班级名，A1 合并改 A1:J1，列宽/边框范围 11→10，行数据去掉 class_name |

### 下一步

- 提交 + 打 tag `v0.3` 发布
- 后端方案（FastAPI + SQLite + uniapp 网页/小程序）出方案文档后单独排期

## 2026-09-13 — v0.3 本地改进（需求讨论 + 实现完成）

### 完成事项

- 通读 needs2.0.md 全部 11 条需求，两轮讨论后确定 v0.3 范围
- 明确本轮只做本地改进，后端相关（登录注册 / 服务器历史 / 课程管理 / 小程序 / 联动）全部挂起
- 实现 v0.3 四项本地改进：班级名、配色、日期筛选、输入框间距

### 关键决策

- 班级名：`Record` 新增 `class_name` 字段，必填；费率仍由班级类型决定；配色 / 筛选按班级名。老数据一律按试验数据处理，不做兼容 / 回退逻辑
- Excel：新增"班级名"列，共 11 列（A~K）→ 已于 09-17 修订为不导出班级名
- 背景色：换高对比调色板，扩到 ~12 色
- 日期筛选：全部 / 某月，导出跟随筛选
- 输入框与文字间距收紧

### 文件变更

| 文件 | 操作 | 说明 |
|---|---|---|
| `codes/models.py` | 修改 | Record 新增 class_name 字段，group_key 改为按班级名 |
| `codes/storage.py` | 修改 | 班级名 JSON 序列化/反序列化 |
| `codes/config.py` | 修改 | 调色板换高对比 12 色 |
| `codes/excel_exporter.py` | 修改 | 配色按班级名（班级名列本身于 09-17 移除） |
| `codes/ui/main_window.py` | 修改 | 班级名输入框、日期筛选、必填校验、表单间距收紧 |

### 下一步

- 用户 9/20 统一测试
- 通过后打包 exe + 更新 README
- 后端相关（登录/服务器历史/课程管理/小程序）单独排期

## 2026-07-11 — v0.2 班级费率管理 + 历史记录加载

### 完成事项

- **费率持久化**：`storage.py` 新增 `load_rates()` / `save_rates()`，费率配置保存到 `~/.salary-counter/rates.json`
- **费率管理弹窗**：新建 `ui/rate_dialog.py`，QDialog 内嵌 QTableWidget，支持班级类型的增删改查 + 校验（非空、不重复、时薪 > 0）
- **主窗口集成**：
  - 班级下拉框旁添加"管理"按钮，点击打开费率弹窗
  - 启动时自动加载持久化费率（覆盖 config.py 默认值）
  - 输入新班级时弹窗引导打开费率管理界面，确认后持久化
- **历史记录加载**：新增"加载数据"按钮，选择 JSON 文件加载历史数据，当前有记录时弹窗确认

### 关键决策

- 费率持久化放在 `storage.py` 而非 `config.py`，与 JSON I/O 逻辑统一
- 弹窗内表格直接可编辑（双击进入编辑），不走额外的"编辑模式"切换
- 历史加载只加一个按钮 + 一个槽函数，不复用独立弹窗，保持极简

### 文件变更

| 文件 | 操作 | 说明 |
|---|---|---|
| `codes/storage.py` | 修改 | +load_rates() / +save_rates() |
| `codes/ui/rate_dialog.py` | 新建 | 费率管理弹窗 |
| `codes/ui/main_window.py` | 修改 | 费率管理按钮、加载数据按钮 |

### 下一步

v0.2 全部完成。后续等待用户测试反馈，下一阶段可考虑：
- 更多使用体验优化（键盘快捷键、批量导入等）
- v1.0 uniapp 微信小程序版本
